---
name: beeja-next
description: Continue the next unfinished Beeja language-model milestone using the project state and verification gates.
disable-model-invocation: true
effort: high
---

Read `CLAUDE.md` and `PROJECT_STATE.md`. Identify the first unfinished or partially complete milestone whose prerequisites are satisfied.

Then invoke and follow the equivalent `/beeja-lab <stage>` workflow. Implement only that stage's smallest complete vertical slice, test it, and update the project state and learning log.

Do not skip ahead because a later stage is more interesting. Do not start a long training run automatically.
